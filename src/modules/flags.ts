/**
 * Some handy flags commonly used as options object properties, that allow 
 * to use shorthand property notation `{flag}` istead of `{flag: true}`.
 */ 

export const required = true;
export const index    = true;
export const unique   = true;
export const nullable = true;